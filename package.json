{
  "name": "auto-cms",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": { "auto-cms": "dist/cli.js" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "dev": "tsx src/cli.ts",
    "test": "vitest run",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "auto-cms": "tsx src/cli.ts"
  },
  "dependencies": {
    "adm-zip": "^0.6.0",
    "commander": "^14.0.0",
    "puppeteer-core": "^24.16.0",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@types/adm-zip": "^0.5.7",
    "@types/node": "^24.0.0",
    "tsx": "^4.20.0",
    "typescript": "^5.9.0",
    "vitest": "^3.2.0"
  }
}
